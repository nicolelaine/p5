var x = 280;
var y = -100;
var diameter = 380;

function setup() {
  // put setup code here

  createCanvas(480, 120);
  fill (102);
}

function draw() {
  // put drawing code here

 background (204);
 ellipse (x, y, diameter, diameter);
}