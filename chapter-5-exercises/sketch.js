


function setup() {
  // put setup code here

  createCanvas(windowWidth, windowHeight);
  textSize(64);
  textAlign(CENTER);
  fill (255);
}

function draw() {
  // put drawing code here
background (0);
text (key, 60, 80);
}