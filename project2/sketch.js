function setup() {
  // put setup code here

  createCanvas(480, 120);

}

function draw() {
  // put drawing code here
  background(204);
  rect(160, 30, 260, 20);
    //the ellipse comes after the rectangle
  //because it comes after it in the code
  ellipse(140, 0, 190, 190);

 
}